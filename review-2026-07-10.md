# 統合レビュー記録 (2026-07-10)

対象: obsidian-wasm-image v0.5.0 (develop, 未コミット差分含む)
方法: 4観点のサブエージェントレビュー + high以上は手動でコード裏取り

深刻度: 🔴 high(修正必須) / 🟡 medium / ⚪ low

## 正確性・データ安全性

- **R-01** 🔴 リサイズが片方の軸しか制約しない — `src/converters/webp-converter.ts:68-77`, `avif-converter.ts:44-53`, `canvas-converter.ts:36-45`
  分岐を `width > height` で選ぶため超過軸と一致しないと縮小されない。デフォルト設定(1920x1080)でも 1900x1200 が無変更で素通りする(検証済み)。
- **R-02** 🔴 上書き→リネームが非アトミック — `src/main.ts:315-333`, `src/file-service.ts:172-195`
  `modifyBinary` で元ファイルを書き換えた後に `renameFile`。リネーム失敗時「拡張子.pngで中身WebP」の破損ファイルが残る。ロールバックなし。
- **R-03** 🔴 ファイル名経由のHTMLインジェクション — `src/ui/image-converter-modal.ts:119,122`
  `file.name` を含む文字列を `innerHTML` に代入。細工したファイル名でElectronレンダラにHTML注入可能。
- **R-04** 🔴 モーダルクローズで変換がキャンセルされない — `src/ui/image-converter-modal.ts:55,239-242`
  Xボタンは `resolve(undefined)` + DOM削除のみ。裏でループが vault に書き続け、孤立ファイルと後追い成功通知が残る。
- **R-05** 🔴 変換中に Paste/Import/Drop が有効なまま — `src/ui/image-converter-modal.ts:163,179,191,289`
  `handleFilesSelect` が `selectedFiles` を再代入し、実行中ループの参照がすり替わる(二重処理・スキップ・undefined)。

## 依存関係・ビルド

- **R-06** 🔴 `safe-chain-test` 依存の混入 — `package.json`
  npm の security holding package(空パッケージ)。import 箇所ゼロ。コミット d3ee452 で混入。削除すべき。
- **R-07** 🔴 WebP WASM の SIMD/非SIMD 両方が Base64 埋め込みされ main.js が約2MB — `esbuild.config.mjs:55-108`
  実行時に使われるのは片方のみ。起動のたびに全量パースされる。
- **R-08** 🟡 production ビルドでも minify 未指定 — `esbuild.config.mjs:179-201`
- **R-09** 🔴 AVIF WASM バージョンのハードコード(2.1.1)と `^2.1.1` の乖離 — `src/converters/avif-wasm-loader.ts:3-5`
  `npm update` で JS グルーと WASM バイナリのバージョンがズレ、AVIF だけ壊れる。
- **R-10** 🔴 AVIF マルチスレッド版グルーへの自動分岐とシングルスレッド版バイナリの不整合リスク — `@jsquash/avif/encode.js` の `threads()` 分岐
  要実機検証(devtools で `threads()` の値を確認)。true なら LinkError の経路がある。
- **R-11** 🟡 AVIF 初期化ガードが check-then-act — `src/converters/avif-converter.ts:16-21`, `avif-wasm-loader.ts:33-61`
  並行呼び出しで二重ダウンロード・二重書き込み。Promise を同期代入するパターンにすべき。

## Obsidian API 規約

- **R-12** 🔴 `vault.adapter` 直叩きで Vault インデックス未反映 — `src/file-service.ts:76-80,104-109,190-192`, `src/main.ts:327-329`
  直後の `![[...]]` が未解決になる。モバイルでは解決しない恐れ。`vault.createBinary`/`createFolder` へ。
- **R-13** 🟡 `normalizePath()` 未使用でユーザー入力フォルダをそのまま結合 — `src/file-service.ts:38,72-73,99-101,186-187`
  Windows のバックスラッシュ入力・末尾スラッシュで不正パス。
- **R-14** 🟡 非公開API `(editor as any).posAtMouse(evt)` 依存 — `src/main.ts:358`

## リソースリーク

- **R-15** 🟡 `<img>` フォールバックの ObjectURL 未 revoke — converters 3ファイル(webp:56-62, avif:32-38, canvas:24-30)
- **R-16** 🟡 `webp-predictor.ts:47-90` の ObjectURL 未 revoke(デフォルト経路、設定変更のたびに蓄積)
- **R-17** 🟡 DropZone プレビューURLがモーダルクローズ時に未解放 — `src/ui/components/drop-zone.ts:97-99,166-169`
- **R-18** 🟡 `ImageBitmap.close()` 未呼び出し — converters 3ファイル

## 設定・プリセット(未コミット差分関連)

- **R-19** 🟡 プリセット重複名バリデーション欠如 — `src/settings-tab.ts:459-497`
  同名プリセットを作成可能。名前引き `find` は先頭のみ。"Photo" 等を付けると勝手にビルトイン扱い。
- **R-20** 🟡 アップグレード時の名前衝突 — `src/main.ts:114-123`, `src/settings.ts:98-104`
  旧版で "Photo" 等の自作プリセットを持つユーザーは、それがロック(削除・改名不可)される。名前ベース判定の限界。
- **R-21** 🟡 リセット確認モーダルの文言が旧仕様 — `src/settings-tab.ts:264`
  「Keep only the Default preset」と表示するが実際は4つのビルトインを復元。
- **R-22** 🟡 Quality 入力の表示/保存値の無言乖離 — `src/settings-tab.ts:402-413,433-457`
  範囲外入力時、フィールドは入力値のまま・保存は旧値。フィードバックなし。
- **R-23** 🟡 モーダルの Max Width/Height に 0 を入力可能 — `src/ui/components/settings-panel.ts:136-158`
  `getImageData(0,0,0,0)` が IndexSizeError で変換全体が失敗。

## 機能の欠陥・不整合

- **R-24** 🟡 tiff が対象拡張子だが Chromium はデコード不可 — `src/settings.ts:123`, `src/main.ts:76`
  必ず失敗する「対応形式」。
- **R-25** 🟡 サイズ予測に世代管理なし — `src/ui/image-converter-modal.ts:88-129`
  連続変更時に古い結果が新しい表示を上書きしうる。
- **R-26** ⚪ `setupAutoOrganizeImages` が完全なデッドコード — `src/main.ts:608-725`
  どこからも呼ばれず UI トグルもなし。配線しても `getAbstractFileByPath` が null で即例外になる実装。
- **R-27** ⚪ `PreviewArea` 未使用 — `src/ui/components/preview-area.ts`(import のみ、配線されれば URL リーク持ち)
- **R-28** ⚪ `saveOriginalFile` の拡張子フォールバック不発 — `src/file-service.ts:99`
  `split('.').pop() || 'unknown'` はドットなしファイル名で fallback しない。

## コード品質

- **R-29** ⚪ `handleAutoConvert`/`handleAutoPaste` がほぼ同一(~90行x2) — `src/main.ts:425-604`
- **R-30** ⚪ 変換 switch 文が3箇所に重複、`main.convertImage` は `convertAndReplaceFile` とほぼ同一 — `src/main.ts:261-339`, `src/file-service.ts`
- **R-31** ⚪ 独自モーダルが Obsidian `Modal` 基底クラス未使用 — `src/ui/image-converter-modal.ts:17-34`
  ESC で閉じない、オーバーレイなし、多重オープン可。
- **R-32** ⚪ `console.log` 残置(Auto-Organize 系)、`closeBtn.innerHTML` に静的 SVG — 公式レビューガイドライン抵触
- **R-33** ⚪ tsconfig `ES2020` と esbuild `es2022` の target 不一致、変換パスの `as any` 多用

## 良好だった点

- イベント登録はすべて `registerEvent` 経由で解放漏れなし
- `preventDefault` のタイミングは適切(非同期処理前、対象外ファイルは他プラグインに委譲)
- CSS は `.wasm-image-*` で名前空間化、テーマ変数使用、`!important` なし
- AVIF WASM の実行時ダウンロード方式自体は起動時間の観点で妥当
- `gif-check.ts` の GIF89a パーサーは破損ファイルにも安全側
